import React from "react";
import ReactDOM from "react-dom/client";

function App(){
  return <div style={{padding:20}}>请把你的完整代码粘贴到这里</div>
}

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
